# Postgres connection string for the Sakenowa-mirrored database.
# Use the EU pooler URL when deploying on Vercel — the serverless model
# spawns many concurrent function instances and would blow through a
# direct connection's max_connections quickly.
DATABASE_URL=

# Optional. One of: silent | error | info | debug. Default: error.
MCP_LOG_LEVEL=
